# -*- coding: utf-8 -*-
# Copyright {{year}} Red Hat
# GNU General Public License v3.0+
# (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)

from __future__ import absolute_import, division, print_function


__metaclass__ = type

"""
The {{Resource}} parser templates file. This contains
a list of parser definitions and associated functions that
facilitates both facts gathering and native command generation for
the given network resource.
"""

import re

from ansible_collections.ansible.netcommon.plugins.module_utils.network.common.rm_base.network_template import (
    NetworkTemplate,
)


class {{Resource}}Template(NetworkTemplate):
    def __init__(self, lines=None, module=None):
        super({{Resource}}Template, self).__init__(lines=lines, tmplt=self, module=module)

    # fmt: off
    PARSERS = [
        # TODO: Add parser definitions for each config attribute
        #
        # Each parser entry needs:
        #   name    — matches a key in the config class's self.parsers list
        #   getval  — compiled regex to extract values from running-config lines
        #   setval  — Jinja2 template to generate the CLI command
        #   result  — dict mapping regex groups to structured data keys
        #
        # Example for a simple attribute:
        # {
        #     "name": "attribute_name",
        #     "getval": re.compile(
        #         r"""
        #         ^command\s(?P<attribute_name>\S+)
        #         $""", re.VERBOSE,
        #     ),
        #     "setval": "command {{ attribute_name }}",
        #     "result": {
        #         "attribute_name": "{{ attribute_name }}",
        #     },
        # },
        #
        # Example for a nested attribute:
        # {
        #     "name": "parent.child",
        #     "getval": re.compile(
        #         r"""
        #         ^parent\s(?P<child>\S+)
        #         $""", re.VERBOSE,
        #     ),
        #     "setval": "parent {{ parent.child }}",
        #     "result": {
        #         "parent": {
        #             "child": "{{ child }}",
        #         },
        #     },
        # },
    ]
    # fmt: on
